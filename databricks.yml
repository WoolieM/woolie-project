bundle:
  name: woolie-data-platform

workspace:
  host: https://dbc-df535374-cdf8.cloud.databricks.com

resources:
  jobs:
    run_test_notebook:
      name: "My-First-Serverless-Test"
      tasks:
        - task_key: "notebook_task"
          # These lines must be perfectly aligned under task_key
          notebook_task:
            notebook_path: "/Users/wooliterchen@gmail.com/Test"

# This defines environment (Dev vs Prod)
targets:
  dev:
    default: true
    mode: development