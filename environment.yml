name: woolie-platform-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pip
  - pytest
  - python-dotenv
  - pandas
  - sqlmodel
  - ipykernel #Option only for test purpose
  - terraform
  - google-cloud-storage
  - google-cloud-bigquery
  - dbt-bigquery
  - google-cloud-sdk
  - pip:
    - fastapi[standard]